/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "standalone",
  // Harici görseller için örnek (gerekirse hostname ekleyin):
  // images: { remotePatterns: [{ protocol: "https", hostname: "example.com", pathname: "/**" }] },
};

export default nextConfig;
